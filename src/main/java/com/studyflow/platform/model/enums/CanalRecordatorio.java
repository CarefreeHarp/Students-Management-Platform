package com.studyflow.platform.model.enums;

/** Canal por el que se entrega un recordatorio. */
public enum CanalRecordatorio {

    /** WhatsApp Cloud API de Meta. */
    WHATSAPP,
    /** Reservado para una futura integracion de correo. */
    CORREO,
    /** Aviso dentro de la propia aplicacion. */
    APP
}

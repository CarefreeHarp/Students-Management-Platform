package com.studyflow.platform.model.enums;

/** Origen de la cuenta: formulario propio o proveedor social (pantalla de login). */
public enum ProveedorAcceso {
    CORREO,
    GOOGLE,
    GITHUB,
    FACEBOOK
}

package com.studyflow.platform.model.enums;

/** Clase de canal dentro del espacio de un proyecto. */
public enum TipoCanal {

    /** Canal principal creado con el proyecto: #general. */
    GENERAL,
    /** Canal dedicado a una tarea concreta: #tarea-prototipo. */
    TAREA,
    /** Canal temático creado a mano por el equipo. */
    LIBRE
}
